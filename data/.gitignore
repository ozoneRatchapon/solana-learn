# ข้อมูลที่ห้ามขึ้น repo สาธารณะ — ดูเหตุผลในหัวไฟล์ entities.yml
#
# ที่นี่คือที่สำหรับ: node ที่เป็นคน, ช่องทางติดต่อส่วนตัว, บันทึกการคุย,
# และการประเมินตัวบุคคล/องค์กร ซึ่งเป็นข้อมูลที่มีค่าที่สุดสำหรับ community operator
# แต่เสียหายที่สุดถ้าหลุด
#
# ใช้ schema เดียวกับ entities.yml ได้ แล้วอ้างถึงกันด้วย id
private.yml
private.*.yml
*.local.yml
